[Kurssi | id Serial (PK) ; kurssinimi varchar(50) ; kuvaus varchar(255) ; aloitusPvm Date ; lopetusPvm Date ; vastuuYksikkoId (FK Vastuuyksikko.id)]<1-*[Opetusaika | id Serial (PK) ; viikonpaiva Integer ; aloitusAika varchar(5) ; lopetusAika varchar(5); kurssiId (FK Kurssi.id) ; tyyppi INTEGER Default 0], [Vastuuyksikko | id Serial (PK) ; nimi varchar(100)]<1-*[Kurssi | id Serial (PK) ; kurssinimi varchar(50) ; kuvaus varchar(255) ; aloitusPvm integer ; lopetusPvm integer ; vastuuYksikkoId (FK Vastuuyksikko.id)],[Kayttaja | id Serial (PK) ; tyyppi INTEGER Default 0 ; nimi varchar(100) ; salasana varchar(255)]<1-*[Kurssisuoritus | id Serial (PK) ; kurssiId (FK Kurssi.id) ; kayttajaId (FK Kayttaja.id) ; arvosana INTEGER Default 0 ; paivays integer], [Kayttaja | id Serial (PK) ; tyyppi INTEGER Default 0 ; nimi varchar(100) ; salasana varchar(255) ; suola varchar(255)]<1-*[KurssiIlmoittautuminen | kurssiId (FK Kurssi.id) ; kayttajaId (FK Kayttaja.id)], [KurssiIlmoittautuminen | kurssiId (FK Kurssi.id) ; kayttajaId (FK Kayttaja.id)]*-1>[Kurssi | id Serial (PK) ; kurssinimi varchar(50) ; aloitusPvm integer ; lopetusPvm integer ; vastuuYksikkoId (FK Vastuuyksikko.id)], [Kurssisuoritus | id Serial (PK) ; kurssiId (FK Kurssi.id) ; kayttajaId (FK Kayttaja.id) ; arvosana integer Default 0 ; paivays integer]*-1>[Kurssi | id Serial (PK) ; kurssinimi varchar(50) ; kuvaus varchar(255) ; aloitusPvm integer ; lopetusPvm integer ; vastuuYksikkoId (FK Vastuuyksikko.id)]