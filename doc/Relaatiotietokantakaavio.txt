[Kurssi | id Serial (PK) ; kurssinimi varchar(50) ; kuvaus varchar(255) ; opintoPisteet INTEGER DEFAULT 5 ; arvosteluTyyppi DEFAULT 0 ; aloitusPvm INTEGER ; lopetusPvm INTEGER ; vastuuYksikkoId (FK Vastuuyksikko.id)]<1-*[Opetusaika | id Serial (PK) ; huone varchar(10) NOT NULL ; viikonpaiva Integer ; aloitusAika INTEGER ; lopetusAika INTEGER; kurssiId (FK Kurssi.id) ; tyyppi INTEGER Default 0], [Vastuuyksikko | id Serial (PK) ; nimi varchar(100)]<1-*[Kurssi | id Serial (PK) ; kurssinimi varchar(50) ; kuvaus varchar(255) ; opintoPisteet INTEGER DEFAULT 5 ; arvosteluTyyppi DEFAULT 0 ; aloitusPvm INTEGER ; lopetusPvm INTEGER ; vastuuYksikkoId (FK Vastuuyksikko.id)],[Kayttaja | id Serial (PK) ; tyyppi INTEGER Default 0 ; nimi varchar(100) NOT NULL UNIQUE ; salasana varchar(255) NOT NULL]<1-*[Kurssisuoritus | id Serial (PK) ; kurssiId (FK Kurssi.id) ; kayttajaId (FK Kayttaja.id) ; arvosana INTEGER Default 0 ; paivays integer], [Kayttaja | id Serial (PK) ; tyyppi INTEGER Default 0 ; nimi varchar(100) NOT NULL UNIQUE ; salasana varchar(255) NOT NULL]<1-*[KurssiIlmoittautuminen | id Serial (PK) ; kurssiId (FK Kurssi.id) ; kayttajaId (FK Kayttaja.id)], [KurssiIlmoittautuminen | id Serial (PK) ; kurssiId (FK Kurssi.id) ; kayttajaId (FK Kayttaja.id)]*-1>[Kurssi | id Serial (PK) ; kurssinimi varchar(50) ; kuvaus varchar(255) ; opintoPisteet INTEGER DEFAULT 5 ; arvosteluTyyppi DEFAULT 0 ; aloitusPvm INTEGER ; lopetusPvm INTEGER ; vastuuYksikkoId (FK Vastuuyksikko.id)], [Kurssisuoritus | id Serial (PK) ; kurssiId (FK Kurssi.id) ; kayttajaId (FK Kayttaja.id) ; arvosana integer Default 0 ; paivays integer]*-1>[Kurssi | id Serial (PK) ; kurssinimi varchar(50) ; kuvaus varchar(255) ; opintoPisteet INTEGER DEFAULT 5 ; arvosteluTyyppi DEFAULT 0 ; aloitusPvm INTEGER ; lopetusPvm INTEGER ; vastuuYksikkoId (FK Vastuuyksikko.id)]
[KurssiIlmoittautuminen | id Serial (PK) ; kurssiId (FK Kurssi.id) ; kayttajaId (FK Kayttaja.id)]-0..1>[HarjoitusRyhmaIlmoittautuminen | id Serial (PK) ; kurssiIlmoId (FK KurssiIlmoittautuminen.id) ; opetusaikaId (FK Opetusaika.id)]
